version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd src/app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: src/app/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*